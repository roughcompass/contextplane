apiVersion: v1
kind: Service
metadata:
  name: {{ include "contextplane.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "contextplane.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "contextplane.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
