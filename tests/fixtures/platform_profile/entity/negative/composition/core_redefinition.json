{
  "why": "The tenant restates a core property exactly as the core has it, weakening nothing. Still refused: the core's meaning would then be written down in two places, free to drift the moment either moves.",
  "expect_conflict_codes": ["core_redefinition"],
  "extension": {
    "namespace": "northwind",
    "target_core_digest": "@core",
    "added_properties": {
      "core:component": [
        {"name": "name", "value_type": "string", "required": true, "min_cardinality": 1, "max_cardinality": 1}
      ]
    }
  }
}
