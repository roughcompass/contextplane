# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Build artifacts
dist/
build/

# OS
.DS_Store

# Local env (operator-provided secrets). .env.example is the tracked spec.
.env
.env.*
!.env.example

# Local dev stack: Postgres data directory, service logs, PID state, and
# the generated environment file. Entirely reproducible from `make dev-up`.
#
# Both spellings, and the second one is not redundant. A trailing slash matches
# a directory only, so a *symlink* named `.devstack` -- which is how a linked
# git worktree borrows the primary checkout's staged embedding artifact -- was
# staged by `git add -A` and committed as an absolute path to one machine. It
# shows up nowhere except the commit stat, and the untracked symlink also blocks
# any tooling that requires a clean tree.
.devstack
.devstack/

# Coverage data. The unit target writes these on every run (the ratchet in
# CLAUDE.md's Testing section); they are machine-local measurements, not
# artifacts, and a dirty tree after `make test-unit` makes every later
# `git status` read as uncommitted work.
.coverage
.coverage.*

# Evaluation campaign output. A signed evidence document is a run artefact, not
# source: committing one would put a measurement into the history of the thing
# it measures, and the coordinator archives the artefact in the planning record
# instead.
run/
